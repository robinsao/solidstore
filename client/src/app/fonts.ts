import { Ruda, Saira } from "next/font/google";

export const ruda = Ruda({ subsets: ["latin"], variable: "--font-ruda" });
export const saira = Saira({ subsets: ["latin"], variable: "--font-saira" });
